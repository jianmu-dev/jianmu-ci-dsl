groups:
  - name: 默认分组
    projects:
      # 批量导入
      - /batch_import.yml
      # 开发工具
      - /dev-tools/docker_image_builder.yml
      # ISSUE通知
      - /ci/issue_notice.yml
  - name: 建木CI
    projects:
      - /ci/server_cicd.yml
      - /ci/ui_cicd.yml
  - name: 建木Hub
    projects:
      # 建木API网关
      - /api-gw/cicd.yml
      # 建木Hub
      - /hub/server_cicd.yml
      - /hub/ui_cicd.yml
      # 建木新版SSO
      - /sso/v2_cicd.yml
      # 建木短信
      - /sms/cicd.yml
  - name: 代码格式化
    projects:
      # prettier前端代码格式化
      - /code-format/prettier_cicd.yml
  - name: 建木运营
    projects:
      # 建木官网
      - /official-site/cicd.yml
      # 建木博客
      - /blog/cicd.yml
  - name: 建木开源治理：jianmu-dev
    projects:
      # 自动评论pr
      - /dev-governance/comment.yml
      # 构建贡献者列表
      - /dev-governance/contributors.yml
      # 构建组织列表
      - /dev-governance/teams.yml
